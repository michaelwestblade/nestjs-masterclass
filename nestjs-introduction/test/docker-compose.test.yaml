version: '3.9'

services:
  test-postgres:
    image: postgres:14-alpine
    ports:
      - 5433:5432
    volumes:
      - db:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=admin
      - POSTGRES_USER=admin
      - POSTGRES_DB=nestjs-blog-test
volumes:
  db:
    driver: local